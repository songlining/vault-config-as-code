# SCIM Bridge Configuration
# Copy this file to .env and replace placeholder values with your actual configuration

# SCIM Authentication
# Bearer token for authenticating SCIM requests from EntraID
# This token must match the one configured in your EntraID SCIM provisioning app
# Example: A strong random token like a UUID or randomly generated string
SCIM_BEARER_TOKEN=your-secure-bearer-token-here

# Git Repository Configuration
# GitHub repository URL where identity YAML files will be committed
# Supports both HTTPS and SSH formats:
#   HTTPS: https://github.com/your-org/your-vault-config-repo.git
#   SSH: git@github.com:your-org/your-vault-config-repo.git
GIT_REPO_URL=https://github.com/your-org/vault-config-as-code.git

# GitHub API Authentication
# Personal access token with repo permissions for creating pull requests
# Create at: https://github.com/settings/tokens (classic token)
# Required permissions: repo (Full control of private repositories)
GITHUB_TOKEN=your-github-token-here

# Application Configuration
# Log level for the SCIM bridge application
# Supported levels: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# File System Paths
# Directory where the Git repository will be cloned locally
# This directory will be created if it doesn't exist
# Must be writable by the application user
REPO_CLONE_DIR=/data/repo

# JSON file for persisting SCIM ID to Vault identity name mappings
# Used for PATCH/DELETE operations that reference users by SCIM ID
# Must be writable by the application user
USER_MAPPING_FILE=/data/user_mapping.json

# Optional: Override default schema path
# Path to the EntraID human identity YAML schema file
# Defaults to ./identities/schema_entraid_human.yaml relative to repo root
# SCHEMA_PATH=/data/repo/identities/schema_entraid_human.yaml

# Optional: Customize commit message prefix
# Prefix for Git commit messages when creating/updating identities
# COMMIT_PREFIX=SCIM:

# Optional: Customize branch name prefix
# Prefix for Git branch names when creating pull requests
# BRANCH_PREFIX=scim-provision